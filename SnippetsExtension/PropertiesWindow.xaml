<Window x:Class="SnippetsExtension.PropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Properties" Height="220" Width="180"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="10,10,10,0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10,10,10,0"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <CheckBox x:Name="snippets" Checked="Snippets_Checked" Unchecked="Snippets_Checked" Content="Enable snippets"/>
        <CheckBox x:Name="brackets" Checked="Brackets_Checked" Unchecked="Brackets_Checked" Content="Enable bracket tools"/>
        <CheckBox x:Name="spaces" Checked="Spaces_Checked" Unchecked="Spaces_Checked" Content="Enable smart spaces"/>
        <Button Click="Snippets_Click" Content="Open snippets file"/>
        <Button Click="Brackets_Click" Content="Open brackets file"/>
    </StackPanel>
</Window>
